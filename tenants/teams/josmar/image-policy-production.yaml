---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: josmar-prod-ip
  namespace: flux-josmar
spec:
  imageRepositoryRef:
    name: josmar-ir
  filterTags:
    pattern: '^v(?P<version>.*)'
    extract: '$version'
  policy:
    semver:
      range: '>=0.0.0-0'
